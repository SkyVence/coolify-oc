{
  "$schema": "https://opencode.ai/config.json",
  // The dev plugin is installed globally from this working tree. There is no
  // project-local plugin override or packaged-plugin disablement anymore.
  // The standard npm plugin remains documented in the global config as the
  // rollback option.
}
